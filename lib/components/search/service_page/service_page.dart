import 'package:flutter/material.dart';

class ServicePage extends StatelessWidget {
  final List services;
  ServicePage({this.services});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}